# 🔐 خطوات تفعيل تسجيل الدخول بجوجل (Google Login)

عشان تشغل زرار "تسجيل الدخول بجوجل" للعملاء، لازم تعمل "ربط" بين جوجل و Supabase. الخطوات بسيطة:

## 1️⃣ الحصول على مفاتيح جوجل (من Google Cloud Console)
1. ادخل على موقع [Google Cloud Console](https://console.cloud.google.com/).
2. اعمل مشروع جديد (New Project) وسميه مثلاً `Diesel Shop`.
3. من القائمة الجانبية، اختار **APIs & Services** ثم **OAuth consent screen**.
   - اختر **External** ثم اضغط **Create**.
   - املأ البيانات الأساسية (اسم التطبيق: `Diesel Shop`، الايميل الخاص بك للدعم والمطور).
   - اضغط **Save and Continue** لحد ما تخلص.
4. من القائمة الجانبية، اختار **Credentials**.
5. اضغط على زر **Create Credentials** واختار **OAuth client ID**.
6. في نوع التطبيق (Application type)، اختار **Web application**.
7. (مهم جداً) في خانة **Authorized redirect URIs**، انسخ الرابط ده وحطه:
   
   ```
   https://ymdnfohikgjkvdmdrthe.supabase.co/auth/v1/callback
   ```
   
8. اضغط **Create**.
9. هيظهرلك نافذة فيها **Your Client ID** و **Your Client Secret**. (ما تقفلش النافذة دي أو انسخهم).

---

## 2️⃣ تفعيل جوجل في Supabase
1. ارجع لصفحة مشروعك في **Supabase**.
2. من القائمة الجانبية، اختار **Authentication** ثم **Providers**.
3. اضغط على **Google**.
4. فعل الزرار (خليه **Enabled**).
5. انسخ الـ **Client ID** من جوجل وحطه في خانة `Client ID` في Supabase.
6. انسخ الـ **Client Secret** من جوجل وحطه في خانة `Client Secret` في Supabase.
7. اضغط **Save**.

---

## 🚀 النتيجة
دلوقتي الكود اللي في الموقع جاهز. بمجرد ما تخلص الخطوتين دول، لما العميل يضغط على "تسجيل الدخول بجوجل"، هيفتحله شاشة جوجل ويسجل عادي جداً، واسمه هيظهر في الموقع!
